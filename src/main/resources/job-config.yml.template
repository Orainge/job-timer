job:
  api:
    enable: true # 是否开启 API 控制，默认为不开启
    token: abcdefg # 调用的token
    url:
      # API 执行路径
      jobName1: /jobName1
  task:
    # 定时配置任务配置
    enable: false # 是否开启定时任务，默认为不开启
    cron:
      jobName1: 0 0/10 * * * ?